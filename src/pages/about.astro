---
import H2 from "../components/H2.astro";
import Main from "../components/Main.astro";
import Layout from "../layouts/Layout.astro";
import Photo from "../components/photo/Photo.astro";
import { t } from "astro-i18next";
---

<Layout title={t("about.title")}>
  <Main class="top-[20%] lg:top-[25%]">
    <div class="flex flex-col justify-center">
      <div class="flex items-center space-x-8 mb-8">
        <div class="prose prose-lg lg:prose-xl text-white">
          <H2 text={t("about.heading")} />
          <p>
            {t("about.intro1")}
          </p>
          <p>
            {t("about.intro2")}
          </p>
          <p>
            {t("about.intro3")}
          </p>
          <p>
            {t("about.intro4")}
          </p>
        </div>
        <Photo src="about" class="max-h-[500px] hidden lg:flex" />
      </div>

      <div class="my-4 prose prose-lg lg:prose-xl text-white mb-20">
        <p>
          {t("about.passion")}
        </p>
        <p>
          {t("about.reading")}
        </p>
        <H2 text={t("about.why_work")} />
        <p>
          {t("about.work_description")}
        </p>
        <H2 text={t("about.get_in_touch")} />
        <p>
          {t("about.contact_intro")}
        </p>
        
        <div class="bg-zinc-800 p-6 rounded-xl shadow-lg w-full">
          <form id="contactForm" class="space-y-4">
            <div>
              <label for="name" class="block text-sm font-medium text-zinc-300 mb-2">{t("about.contact_form.name")}</label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-3 py-2 bg-zinc-700 border border-zinc-600 rounded-lg text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-zinc-500"
                placeholder={t("about.contact_form.name_placeholder")}
              />
            </div>
            
            <div>
              <label for="message" class="block text-sm font-medium text-zinc-300 mb-2">{t("about.contact_form.message")}</label>
              <textarea
                id="message"
                name="message"
                rows="4"
                required
                class="w-full px-3 py-2 bg-zinc-700 border border-zinc-600 rounded-lg text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-zinc-500"
                placeholder={t("about.contact_form.message_placeholder")}
              ></textarea>
            </div>
            
            <button
              type="submit"
              class="w-full bg-zinc-600 hover:bg-zinc-500 text-white font-medium py-2 px-4 rounded-lg transition-colors"
            >
              {t("about.contact_form.send")}
            </button>
          </form>
        </div>
        
        <p class="text-sm text-zinc-400 mt-4">
          {t("about.email_direct")} <a href="mailto:cdanielraducu@gmail.com" class="text-white font-medium hover:underline">cdanielraducu@gmail.com</a>
        </p>
        
        <p class="text-white">
          {t("about.blessing")}
        </p>
      </div>
    </div>
  </Main>
</Layout>

<script>
  document.getElementById('contactForm')?.addEventListener('submit', function(e) {
    e.preventDefault();
    
    const name = (document.getElementById('name') as HTMLInputElement).value;
    const message = (document.getElementById('message') as HTMLTextAreaElement).value;
    
    const subject = `Message from ${name}`;
    const body = `Hi Dani,\n\n${message}\n\nBest regards,\n${name}`;
    
    const mailtoLink = `mailto:cdanielraducu@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
    
    window.location.href = mailtoLink;
  });
</script>
